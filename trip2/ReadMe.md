```定义和大小
   数组（Array）：
   数组的大小是固定的，一旦声明，其大小就不能改变。
   数组在声明时需要指定其长度。
   例如：var arr [5]int 声明了一个长度为 5 的整数数组。
   
   切片（Slice）：
   切片的大小是动态的，可以根据需要增长或缩小。
   切片是基于数组构建的，但它可以动态地调整大小。
   例如：var slice []int 声明了一个整数切片。